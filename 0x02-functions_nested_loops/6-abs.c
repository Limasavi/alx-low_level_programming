#include "main.h"

/**
 * _abs - computes the absolute value of an integer
 * @c: number computed
 * return: absolute value
 */
int _abs(int)
{
	if (c < 0)
	{
		int abs_val;

		abs_val = c * -1;
		return (abs_val);
	}
	return (c);
}
